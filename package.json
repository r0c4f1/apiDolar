{
  "name": "tu-proyecto",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "build": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "1.40.0",
    "express": "4.18.0"
  }
}