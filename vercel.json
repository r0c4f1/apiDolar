{
  "version": 2,
  "builds": [
    {
      "src": "*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./server.js"
    }
  ],
  "installCommand": "npm install && npx playwright install chromium"
}